name: parmesan
description: "A CLI tool that scaffolds Flutter + C++ FFI projects with automatic Dart binding generation, module management, and cross-platform CMake configuration for Windows and Linux."
version: 0.2.1
repository: https://github.com/invok3/parmesan

environment:
  sdk: ^3.11.5

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  test: ^1.31.1
  lints: ^6.1.0

platforms:
  linux:
  windows: